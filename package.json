{
  "name": "copa-bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "NODE_PATH=lib nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "pretest": "npm run lint",
    "lint-init": "eslint --init",
    "lint": "eslint index.js lib/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "botkit": "^0.2.2",
    "dotenv": "^2.0.0",
    "got": "^6.5.0"
  },
  "devDependencies": {
    "eslint": "^3.7.0",
    "nodemon": "^1.10.2"
  }
}
